// Use variable `input`
const problem = input;
const solution = solve(problem);

// Return something
return solution;

// Helper
function solve(input) {
    return `foo-${input.length}`;
}
